# v1 files recovered from git history

| File | Source |
|---|---|
| `index.html` | v1 repo (`louisbenne/ArtisanOven`), `git show 0c2a41c^:index.html` = commit `4184ef74319786a3661e3e5d9a1f4cbe80172508` |

v1's current `index.html` is the fully-booked page (swapped in by hand in commit
`0c2a41c`, 23 Sept 2026). This is the normal home page (hero, availability tracker,
I WANT TO ORDER / I NEED TO PAY cards) that `public/index.html` must be copied from.
Read-only: never edit.
